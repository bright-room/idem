---
name: plan
description: GitHub Issue を参照して実装プランを作成する。ローカルでファイルとして出力する。
argument-hint: "[issue-number]"
---

# Implementation Plan Skill

GitHub Issue を参照し、コードベースを調査した上で実装プランを作成する。

## 前提条件

- `gh` CLI が認証済みであること

## 手順

### 1. Issue の取得

引数に Issue 番号を指定する（必須）。

`gh issue view <issue-number>` で Issue のタイトル、本文、ラベル、コメントを取得し、要件を正確に理解する。

### 2. コードベースの調査

Issue の要件に基づいて、関連するコードを徹底的に調査する。

- **既存の実装パターン**: 同様の機能がどのように実装されているかを確認する
- **影響範囲**: 変更が影響するパッケージ、インターフェース、テストを特定する
- **拡張ポイント**: インターフェース（Storage, Locker, Validator）、ミドルウェア、Functional Options など、活用すべき拡張ポイントを確認する
- **テスト構成**: 既存のテスト構成（ユニットテスト、統合テスト）を確認する。統合テストは Docker 内の Redis（Standalone / Cluster / Sentinel）環境で実行される点を考慮する
- **コード生成**: `recorder_gen.go` / `recorder_gen_test.go` は `go generate ./...` で生成されるファイルである。`internal/cmd/genrecorder/` に変更が必要かを確認する

### 3. 実装プランの作成

以下の観点で実装プランを設計する:

- **実現可能性**: 要件が現在のアーキテクチャで実現可能かを検証する
- **設計判断**: 複数のアプローチがある場合は、各アプローチのトレードオフを分析する
- **段階的な実装**: 変更を論理的なステップに分割し、各ステップの依存関係を明確にする
- **テスト戦略**: 各変更に対するテスト方針を策定する

### 4. 実装プランの出力

実装プランを `.claude/outputs/plans/` ディレクトリにファイルとして出力する。

- ディレクトリが存在しない場合は作成すること
- ファイル名: `PLAN-<Issue番号>-<Issueタイトルをケバブケースに変換>.md`
  - 例: Issue #42 "Add middleware support" → `PLAN-42-add-middleware-support.md`
  - タイトルに含まれる特殊文字は除去し、スペースをハイフンに変換する

## 実装プランのフォーマット

以下のフォーマットに従って Markdown を生成する。

````markdown
## 実装プラン

> 📋 Issue: #<issue-number>
> 📅 作成日: YYYY-MM-DD

### 概要

<!-- Issue の要件を簡潔に要約し、実装方針の全体像を述べる -->

### 設計判断 *(省略可: 複数のアプローチがある場合のみ記載)*

| アプローチ | メリット | デメリット |
|-----------|---------|-----------|
| A: <方式名> | <メリット> | <デメリット> |
| B: <方式名> | <メリット> | <デメリット> |

**採用案**: <選択したアプローチとその理由>

### 影響範囲

<!-- パッケージ構成: ルートパッケージ (idem), redis/, internal/cmd/genrecorder/ -->

| パッケージ | 影響 | 備考 |
|-----------|------|------|
| `idem`（ルート） | 新規 / 変更 / なし | <概要> |
| `redis/` | 新規 / 変更 / なし | <概要> |
| `internal/cmd/genrecorder/` | 新規 / 変更 / なし | <概要（変更時は go generate が必要）> |

### ファイル構成

<!-- 変更・追加されるファイルの構成を示す。新規追加は (new)、変更は (modify) で表記する -->

<!-- 実際のプロジェクト構成に合わせてツリーを記述する -->

```
.
├── new_file.go              (new)    ← ルートパッケージ (idem)
├── existing_file.go         (modify)
├── redis/
│   └── new_feature.go       (new)
└── internal/
    └── cmd/
        └── genrecorder/
            └── main.go      (modify)  ← 変更時は go generate が必要
```

### 実装ステップ

<!-- 実装を Phase（論理的なまとまり）と Step（具体的な作業単位）に分割する -->

#### Phase 1: <フェーズのタイトル>

<!-- フェーズの目的・ゴールを簡潔に述べる -->

##### Step 1: <ステップのタイトル>

**対象**: `path/to/file.go`

<!-- 具体的な変更内容を記述する。必要に応じてコード例を含める -->

```go
// コード例（必要な場合のみ）
```

##### Step 2: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

#### Phase 2: <フェーズのタイトル>

##### Step 1: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

### ドキュメント更新

<!-- 実装に伴い更新が必要なドキュメントを記載する -->

| ドキュメント | 更新内容 |
|-------------|---------|
| `README.md` | <更新内容の概要> |
| `CLAUDE.md` | <更新内容の概要> |

### テスト戦略

<!-- ビルド・テストは Docker 内で実行される（make test-unit / make test-integration） -->
<!-- 統合テストは Redis Standalone / Cluster / Sentinel 環境が必要 -->

| テスト種別 | 対象 | テスト内容 | 実行方法 |
|-----------|------|-----------|---------|
| ユニットテスト | `package/func` | <テスト内容> | `make test-unit` |
| 統合テスト | `package/func` | <テスト内容> | `make test-integration`（Redis 環境必要） |

### 確認事項 *(省略可: 確認すべき事項がなければセクションごと省略)*

- [ ] <確認事項1>
- [ ] <確認事項2>

### 今後の展望 *(省略可: スコープ外の関連拡張がなければセクションごと省略)*

- **<拡張テーマ>**: <概要と期待される効果>
- **<拡張テーマ>**: <概要と期待される効果>

---
🤖 *Generated by Claude Code*
````

## 注意事項

- Issue の要件を正確に理解し、過不足のないプランを作成すること
- 推測ではなく、実際にコードを読んで確認した事実に基づいてプランを作成すること
- 実装ステップは具体的に記述し、各ステップで変更するファイルパスを明示すること
- 既存のアーキテクチャパターン（Functional Options、`-er` suffix のインターフェース命名、短いレシーバ名）に従った設計にすること
- 不明点や判断が必要な事項は「確認事項」セクションに明記すること
- コード生成ファイル（`recorder_gen.go`）に関連する変更がある場合は、`go generate` の実行を明記すること
- 変更のない「設計判断」「確認事項」「今後の展望」セクションは省略してよい
- 「今後の展望」は Issue のスコープ外の関連拡張を記載する。実装プラン策定時にコードベースを調査する中で見つけた改善機会や、自然な次のステップとなる機能拡張を挙げること
- 実装に伴うドキュメント（`README.md`、`CLAUDE.md`）の更新を必ず「ドキュメント更新」セクションに記載すること。新機能・設定変更・アーキテクチャ変更がある場合、対応するドキュメントの更新箇所を具体的に特定すること
